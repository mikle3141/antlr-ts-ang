package org.antlr.jetbrains.adaptor.test;

import org.junit.Test;

/**
 * Placeholder test to keep the test source set compiling.
 *
 * The original implementation depended on IntelliJ IDEA's test framework
 * (`ParsingTestCase`) and on sample language classes (`SampleLanguage`,
 * `SampleParserDefinition`) that are not present in this project anymore.
 *
 * If you later add proper test infrastructure and language classes, you can
 * replace this placeholder with real tests.
 */
public class TestXPath {

    @Test
    public void dummyTest() {
        // No-op test. Exists only so that `./gradlew test` succeeds.
    }
}
